#!/bin/bash
# ABOUTME: Per-challenge cleanup. Kill the Worker and the starter.
# macOS pkill does not support alternation. One pattern per line.
set -euo pipefail

pkill -f "start_worker"   2>/dev/null || true
pkill -f "start_workflow" 2>/dev/null || true
sleep 1

echo "Exercise 1 cleanup: OK"
